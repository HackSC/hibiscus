org: hacksc
app: python-testsgk
service: python-testsgk
plugins:
  - serverless-python-requirements
  - serverless-wsgi
custom:
  wsgi:
    app: python_testsgk/app.app
    packRequirements: false
package:
  individually: false
  patterns:
    - '!**'
    - 'python_testsgk/**'
provider:
  name: aws
  runtime: python3.9
  region: us-west-1
functions:
  api:
    handler: wsgi_handler.handler
    environment: ${file(configs/env.${opt:stage,'local'}.yml)}
    events:
      - http: ANY /
      - http: ANY /{proxy+}
