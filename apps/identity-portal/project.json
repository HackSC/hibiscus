{
  "name": "identity-portal",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/identity-portal",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/identity-portal",
        "outputPath": "dist/apps/identity-portal"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/identity-portal"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "identity-portal:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "identity-portal:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "identity-portal:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "identity-portal:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/identity-portal/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/identity-portal/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
