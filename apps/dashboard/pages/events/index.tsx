import styled from 'styled-components';
import EventsCalendar from '../../components/events/events-calendar';

export function Index() {
  return <Calendar />;
}

export default Index;

const Calendar = styled(EventsCalendar)``;
