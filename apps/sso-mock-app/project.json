{
  "$schema": "..\\..\\node_modules\\nx\\schemas\\project-schema.json",
  "sourceRoot": "apps/sso-mock-app",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/sso-mock-app",
        "outputPath": "dist/apps/sso-mock-app"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/sso-mock-app"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "sso-mock-app:build",
        "dev": true,
        "port": 7777
      },
      "configurations": {
        "development": {
          "buildTarget": "sso-mock-app:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "sso-mock-app:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "sso-mock-app:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/sso-mock-app"],
      "options": {
        "jestConfig": "apps/sso-mock-app/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/sso-mock-app/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
