frameworkVersion: 3
service: base

provider:
  name: aws
  runtime: nodejs16.x
  memorySize: 128
  stage: dev
  region: us-west-1
package:
  individually: true
  excludeDevDependencies: true

plugins:
  - serverless-offline
