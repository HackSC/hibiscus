name: Move cards into In Progress when

on:
  pull_request:
    types:
      - 'opened'
      - 'reopened'

jobs:
  automate-project-columns:
    runs-on: ubuntu-latest
    steps:
      - uses: alex-page/github-project-automation-plus@v0.8.1
        with:
          project: Hibiscus
          column: In progress
          repo-token: ${{ secrets.BOT_GITHUB_PAT }}
