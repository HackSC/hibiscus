import { sponsorServiceApi } from './sponsor-service-api';

describe('sponsorServiceApi', () => {
  it('should work', () => {
    expect(sponsorServiceApi()).toEqual('sponsor-service-api');
  });
});
